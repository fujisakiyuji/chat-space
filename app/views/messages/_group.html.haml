.contents__group__title
  = @group.name
.contents__group__menbers
  Menber：
  - @group.users.each do |user|
    = user.name
= link_to "Edit", edit_group_path(@group), class: "contents__group__edit"
